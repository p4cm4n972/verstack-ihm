import { IsMobileOnlyDirective } from './is-mobile-only.directive';

describe('IsMobileOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new IsMobileOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
