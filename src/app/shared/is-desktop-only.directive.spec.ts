import { IsDesktopOnlyDirective } from './is-desktop-only.directive';

describe('IsDesktopOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new IsDesktopOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
